# Upstash Redis Configuration (Optional - for production multiplayer)
# Sign up for free at https://upstash.com/
# Create a Redis database and copy the REST API credentials below

# Required for reliable online multiplayer
UPSTASH_REDIS_REST_URL=your_upstash_redis_rest_url_here
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_rest_token_here

# If these are not set, the app will work locally using in-memory storage
# but online multiplayer will not work reliably due to serverless cold starts
